"""Culler and raw-processing **experiments** (not wired into ``services.job_executor``).

Used by tests and root ``main.py``; the production pipeline goes through
``services.processor`` + ``engine`` operators. See ``docs/REPO_STRUCTURE.md``.
"""
