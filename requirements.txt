# Core pipeline
numpy>=1.24
opencv-python-headless>=4.8
Pillow>=10.0
ImageHash>=4.3
pillow-heif>=0.16.0
PyYAML>=6.0
requests>=2.28
ExifRead>=3.0

# API server
fastapi>=0.115
uvicorn>=0.30
jinja2>=3.1
python-multipart>=0.0.9

# Async queue
celery>=5.4
redis>=5.0

# Production observability (Prometheus metrics + OTLP traces).
prometheus-client>=0.26
opentelemetry-api>=1.44
opentelemetry-sdk>=1.44
opentelemetry-exporter-otlp-proto-http>=1.44
opentelemetry-instrumentation-fastapi>=0.65b0
opentelemetry-instrumentation-requests>=0.65b0
opentelemetry-instrumentation-celery>=0.65b0

# CLIP embeddings for gallery similarity / diversity (optional at runtime if unused)
open-clip-torch>=2.20

# Structured LLM output — Pydantic-validated responses from vLLM / OpenAI-compatible backends
# Used by VLLMProvider.generate_structured() and the instructor usage example in schemas.py.
openai>=1.0
instructor>=1.2

# Gallery chat LangGraph runtime (decide → act → answer).
langgraph>=0.2
